<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <!-- Link to your CSS file -->
    <link href="/path/to/your/style.css" rel="stylesheet">
</head>

<body class="bg-info">
    <!-- Simplified Navigation Bar -->
    <nav class="post-header">
        <div>
            <!-- Home link -->
            <a class="btn-nav" aria-current="page" href="/">Home</a>

            <!-- Dashboard link, visible only if logged in -->
            {{#if logged_in}}
            <a class="btn-nav" aria-current="page" href="/dashboard">Dashboard</a>
            {{/if}}

            <!-- Conditional Login/Logout link based on login status -->
            {{#if logged_in}}
            <a class="btn-nav" href="/logout" id="logout">Log out</a>
            {{else}}
            <a class="btn-nav" href="/login">Log in</a>
            {{/if}}
        </div>
    </nav>

    <!-- Header Text Box -->
    <div class="center">
        <h1>Tech Blog</h1>
    </div>

    <!-- Main content area -->
    <main class="center">
        {{{ body }}}
    </main>

    <!-- Logout script, included only if logged in -->
    {{#if logged_in}}
    <script src="../public/js/logout.js"></script>
    {{/if}}
</body>

</html>